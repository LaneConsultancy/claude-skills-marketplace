I need write permission to save the HTML file. I was trying to write the complete service page to `unslop-output/underfloor-heating.html`.

To proceed, you can either:
1. Run `/permissions` and allow file writes, or
2. Switch out of `dontAsk` mode

The full page is ready — here's a summary of what it contains so you can decide whether to enable writing:

**Structure:** Hero → Intro → Wet vs Electric (two cards) → New Build vs Retrofit → 7-step process → 5 trust points → 6-question FAQ → CTA

**Design:** Self-contained HTML with embedded CSS, no external dependencies, mobile-responsive grid, brand colours (navy/amber), accessible markup. Placeholder `tel:` link in the CTA that needs a real number.

**Copy:** Already written above — UK English, no filler phrases, concrete cost figures with a note to adjust them to your actual pricing.

Once you grant write access, I'll save it in one tool call.